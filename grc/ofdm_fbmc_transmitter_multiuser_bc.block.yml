# auto-generated by grc.converter

id: ofdm_fbmc_transmitter_multiuser_bc
label: FBMC Transmitter (Multiuser)
category: FBMC

parameters:
-   id: M
    label: M
    dtype: int
    default: M
-   id: K
    label: K
    dtype: int
    default: K
-   id: qam_size
    label: qam_size
    dtype: int
    default: qam_size
-   id: syms_per_frame
    label: syms_per_frame
    dtype: int
    default: syms_per_frame
-   id: start
    label: start
    dtype: int
-   id: end
    label: end
    dtype: int
-   id: theta_sel
    label: theta_sel
    dtype: int
    default: theta_sel
-   id: exclude_preamble
    label: exclude_preamble
    dtype: int
    default: exclude_preamble
-   id: sel_preamble
    label: sel_preamble
    dtype: enum
    options: ['0', '1', '2']
    option_labels: [IAM-C, IAM-C with triple rep., IAM-R]
-   id: zero_pads
    label: Zero padding
    dtype: int
    default: '1'
-   id: extra_pad
    label: Extra padding
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']

inputs:
-   domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import ofdm
    make: ofdm.fbmc_transmitter_multiuser_bc(${M}, ${K}, ${qam_size}, ${syms_per_frame},
        ${start}, ${end}, ${theta_sel}, ${exclude_preamble}, ${sel_preamble}, ${zero_pads},
        ${extra_pad})

file_format: 1
