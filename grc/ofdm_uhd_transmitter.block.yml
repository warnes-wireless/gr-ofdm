# auto-generated by grc.converter

id: ofdm_uhd_transmitter
label: UHD Transmitter
category: ofdm

parameters:
-   id: args
    label: Args
    dtype: string
    default: '""'
-   id: bandwidth
    label: Bandwidth
    dtype: float
    default: bandwidth
-   id: frequency
    label: Frequency
    dtype: float
    default: tx_freq
-   id: lo_offset
    label: Local Offset
    dtype: float
    default: '0'
-   id: gain
    label: Tx Gain
    dtype: float
    default: '0'
-   id: spec
    label: Subdev Spec
    dtype: string
    default: None
-   id: antenna
    label: Antenna
    dtype: string
    default: '""'
-   id: clock_source
    label: Clock Source
    dtype: string
    default: None
-   id: time_source
    label: Time Source
    dtype: string
    default: None
-   id: verbose
    label: Verbose
    dtype: bool
    default: 'False'

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: from uhd_interface import uhd_transmitter
    make: uhd_transmitter(${args}, ${bandwidth}, ${frequency}, ${lo_offset}, ${gain},
        ${spec}, ${antenna}, ${clock_source}, ${time_source}, ${verbose})

documentation: |-
    UHD Transmitter
    Wrapper Block

file_format: 1
