# auto-generated by grc.converter

id: ofdm_pilot_subcarrier_filter
label: OFDM Pilot Subcarrier Filter
category: ofdm

parameters:
-   id: itemsize
    label: Itemsize
    dtype: enum
    options: [complex, float]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float]
    hide: part
-   id: data_subcarriers
    label: Data Subcarriers
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: training_data
    label: Training Data
    dtype: raw
-   id: complex_value
    label: Complex Value
    dtype: bool
    default: 'True'

inputs:
-   domain: stream
    dtype: ${ itemsize }
    vlen: ${ subcarriers }

outputs:
-   domain: stream
    dtype: ${ itemsize }
    vlen: ${ data_subcarriers }
asserts:
- ${ data_subcarriers > 0 }
- ${ subcarriers > 0 }

templates:
    imports: from ofdm.preambles_grc import pilot_subcarrier_filter
    make: pilot_subcarrier_filter(${data_subcarriers},${subcarriers},${training_data},${complex_value})

documentation: |-
    OFDM Pilot Subcarrier Filter
    Wrapper Block

file_format: 1
