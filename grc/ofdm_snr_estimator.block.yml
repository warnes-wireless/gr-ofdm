# auto-generated by grc.converter

id: ofdm_snr_estimator
label: OFDM SNR Estimator
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: skip
    label: Skip
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
-   label: out1
    domain: stream
    dtype: float

templates:
    imports: import ofdm
    make: ofdm.snr_estimator(${vlen}, ${skip})

file_format: 1
