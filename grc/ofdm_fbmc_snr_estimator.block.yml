# auto-generated by grc.converter

id: ofdm_fbmc_snr_estimator
label: FBMC SNR Estimator Block
category: FBMC

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: skip
    label: Skip
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen*skip }

outputs:
-   domain: stream
    dtype: float
-   label: out1
    domain: stream
    dtype: float

templates:
    imports: import ofdm
    make: ofdm.fbmc_snr_estimator(${vlen}, ${skip})

file_format: 1
