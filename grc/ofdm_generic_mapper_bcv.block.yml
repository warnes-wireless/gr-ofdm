# auto-generated by grc.converter

id: ofdm_generic_mapper_bcv
label: Generic Mapper
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: coding
    label: Coding
    dtype: raw
    default: '0'
-   id: frame_size
    label: Frame Size
    dtype: int

inputs:
-   label: data
    domain: stream
    dtype: byte
-   label: cv
    domain: stream
    dtype: byte
    vlen: ${ vlen }

outputs:
-   label: blk
    domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import ofdm
    make: ofdm.generic_mapper_bcv(${vlen}, ${coding}, ${frame_size})

file_format: 1
