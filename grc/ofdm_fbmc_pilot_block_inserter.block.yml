# auto-generated by grc.converter

id: ofdm_fbmc_pilot_block_inserter
label: FBMC Pilot Block Inserter
category: FBMC

parameters:
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_data_part
    label: Frame Data Part
    dtype: int
    default: '1'
-   id: training_data
    label: Training Data
    dtype: raw
-   id: ant
    label: Antennas
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
asserts:
- ${ frame_data_part > 0 }
- ${ subcarriers > 0 }
- ${ ant > 0 }

templates:
    imports: from ofdm.preambles_grc import fbmc_pilot_block_inserter
    make: fbmc_pilot_block_inserter(${subcarriers}, ${frame_data_part}, ${training_data},
        ${ant})

documentation: "FBMC Pilot Block Inserter\n\tAdd Cyclic Prefix allways false!\n\t\
    Wrapper Block"

file_format: 1
