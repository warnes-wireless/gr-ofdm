# auto-generated by grc.converter

id: ofdm_multiply_const_vcc
label: Multiply Constant VCC
category: ofdm

parameters:
-   id: k
    label: Constant Vector
    dtype: complex_vector
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ len(k) }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ len(k) }

templates:
    imports: import ofdm
    make: ofdm.multiply_const_vcc(${k})

file_format: 1
