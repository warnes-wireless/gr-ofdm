# auto-generated by grc.converter

id: ofdm_repetition_decoder_bs
label: repetition_decoder_bs
category: ofdm

parameters:
-   id: output_bits
    label: Output Bits
    dtype: short
-   id: repetitions
    label: Repetitions
    dtype: short
-   id: whitener
    label: Whitener
    dtype: int_vector

inputs:
-   domain: stream
    dtype: char

outputs:
-   domain: stream
    dtype: short

templates:
    imports: import ofdm
    make: ofdm.repetition_decoder_bs(${output_bits}, ${repetitions}, ${whitener})

file_format: 1
