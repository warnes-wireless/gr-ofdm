# auto-generated by grc.converter

id: ofdm_time_sync
label: OFDM Time Synchronization
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: cplen
    label: CP Length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
-   domain: stream
    dtype: float
    multiplicity: '2'

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   label: trigger
    domain: stream
    dtype: byte
    vlen: 1

templates:
    imports: import ofdm
    make: ofdm.time_sync(${vlen}, ${cplen})

file_format: 1
