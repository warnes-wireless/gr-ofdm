# auto-generated by grc.converter

id: ofdm_pilot_block_filter
label: OFDM Pilot Block Filter
category: ofdm

parameters:
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_length
    label: Frame Length
    dtype: int
    default: '1'
-   id: training_data
    label: Training Data
    dtype: raw

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
-   domain: stream
    dtype: byte
asserts:
- ${ subcarriers > 0 }

templates:
    imports: from ofdm.preambles_grc import pilot_block_filter
    make: pilot_block_filter(${subcarriers}, ${frame_length}, ${training_data})

documentation: |-
    OFDM Pilot Block Filter
        Wrapper Block

file_format: 1
