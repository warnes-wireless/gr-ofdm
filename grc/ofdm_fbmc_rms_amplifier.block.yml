# auto-generated by grc.converter

id: fbmc_rms_amplifier
label: FBMC RMS Amplifier
category: FBMC

parameters:
-   id: amplitude
    label: Amplitude
    dtype: float
    default: '1'
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: from ofdm.fbmc_rms_amplifier_grc import fbmc_rms_amplifier
    make: fbmc_rms_amplifier(${amplitude}, ${subcarriers})
    callbacks:
    - set_rms_amplitude(${amplitude})

documentation: "FBMC RMS Amplifier\n\tWrapper Block"

file_format: 1
