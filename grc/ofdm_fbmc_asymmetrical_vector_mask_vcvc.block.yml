# auto-generated by grc.converter

id: ofdm_fbmc_asymmetrical_vector_mask_vcvc
label: Assym. Vector Mask
category: FBMC

parameters:
-   id: M
    label: M
    dtype: int
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: start
    label: Start
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: end
    label: End
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ (end-start+1) }

templates:
    imports: import ofdm
    make: ofdm.fbmc_asymmetrical_vector_mask_vcvc(${M}, ${start}, ${end})

file_format: 1
