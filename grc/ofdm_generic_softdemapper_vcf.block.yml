# auto-generated by grc.converter

id: ofdm_generic_softdemapper_vcf
label: Generic Soft Demapper
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_size
    label: Frame Size
    dtype: int
-   id: coding
    label: Coding
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']

inputs:
-   label: sym_in
    domain: stream
    dtype: complex
    vlen: ${ vlen }
-   label: cv_in
    domain: stream
    dtype: byte
    vlen: ${ vlen }
-   label: csi_in
    domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import ofdm
    make: ofdm.generic_softdemapper_vcf(${vlen}, ${frame_size}, ${coding})

file_format: 1
