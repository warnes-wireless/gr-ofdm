# auto-generated by grc.converter

id: ofdm_fbmc_postprocess_CTF_estimate
label: FBMC Postprocess CTF Estimate
category: FBMC

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

templates:
    imports: import ofdm
    make: ofdm.fbmc_postprocess_CTF_estimate(${vlen})

file_format: 1
