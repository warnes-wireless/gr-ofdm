# auto-generated by grc.converter

id: ofdm_mm_frequency_estimator
label: Morelli-Mengali Frequency Estimator
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: identical_parts
    label: Identical Parts
    dtype: int
-   id: scale
    label: Scale
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import ofdm
    make: ofdm.mm_frequency_estimator(${vlen}, ${identical_parts}, ${scale})

file_format: 1
