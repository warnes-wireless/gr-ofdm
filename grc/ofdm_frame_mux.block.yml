# auto-generated by grc.converter

id: ofdm_frame_mux
label: Frame Multiplexer
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_len
    label: Frame Length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import ofdm
    make: ofdm.frame_mux(${vlen}, ${frame_len})

file_format: 1
