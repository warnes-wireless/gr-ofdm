# auto-generated by grc.converter

id: ofdm_allocation_buffer
label: Allocation Buffer
category: ofdm

parameters:
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: subcarriers
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: data_symbols
    label: Data Symbols
    dtype: int
    default: data_symbols
-   id: address
    label: Address
    dtype: string
    default: address
-   id: coding
    label: Coding
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']

inputs:
-   label: in_id
    domain: stream
    dtype: short
    vlen: 1

outputs:
-   label: out_bitcount
    domain: stream
    dtype: int
    vlen: 1
-   label: out_bitloading
    domain: stream
    dtype: byte
    vlen: ${ subcarriers }
-   label: out_power
    domain: stream
    dtype: float
    vlen: ${ subcarriers }

templates:
    imports: import ofdm
    make: ofdm.allocation_buffer(${subcarriers}, ${data_symbols}, ${address}, ${coding})

file_format: 1
