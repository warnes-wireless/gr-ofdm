# auto-generated by grc.converter

id: ofdm_fbmc_junction_vcvc
label: Junction
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: num_input
    label: Num_input
    dtype: raw
    default: '2'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }
    multiplicity: '2'

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_junction_vcvc(${M}, ${num_input})

file_format: 1
