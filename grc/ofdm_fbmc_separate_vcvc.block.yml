# auto-generated by grc.converter

id: ofdm_fbmc_separate_vcvc
label: Separate
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: num_output
    label: Num_output
    dtype: raw
    default: '2'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }
    multiplicity: '2'

templates:
    imports: import ofdm
    make: ofdm.fbmc_separate_vcvc(${M}, ${num_output})

file_format: 1
