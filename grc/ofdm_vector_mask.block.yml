# auto-generated by grc.converter

id: ofdm_vector_mask
label: Vector Mask
category: FBMC

parameters:
-   id: vlen
    label: Vlen
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: cut_left
    label: Cut_left
    dtype: raw
    default: (M-carriers)/2
-   id: mask_len
    label: Mask_len
    dtype: raw
    default: carriers
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: mask
    label: Mask
    dtype: raw
    default: '[]'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ mask_len }

templates:
    imports: import ofdm
    make: ofdm.vector_mask(${vlen}, ${cut_left}, ${mask_len}, ${mask})

file_format: 1
