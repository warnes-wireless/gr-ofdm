# auto-generated by grc.converter

id: ofdm_symbol_random_src
label: OFDM Symbol Random Source
category: ofdm

parameters:
-   id: symbols
    label: Symbols
    dtype: complex_vector
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   domain: stream
    dtype: byte
    vlen: ${ vlen }

templates:
    imports: import ofdm
    make: ofdm.symbol_random_src(${symbols}, ${vlen})

file_format: 1
