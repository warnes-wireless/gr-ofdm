# auto-generated by grc.converter

id: ofdm_fbmc_beta_multiplier_vcvc
label: Beta Multiplier
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: K
    label: K
    dtype: raw
    default: K
-   id: lp
    label: Filter Length
    dtype: raw
    default: K*M-1
-   id: offset
    label: Offset
    dtype: raw
    default: '0'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_beta_multiplier_vcvc(${M}, ${K}, ${lp}, ${offset})

file_format: 1
