# auto-generated by grc.converter

id: ofdm_scfdma_subcarrier_demapper_vcvc
label: Subcarrier Demapper
category: SC-FDMA

parameters:
-   id: M
    label: M
    dtype: int
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: N
    label: N
    dtype: int
    default: N
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: start
    label: Start index
    dtype: int
-   id: mode
    label: Mode
    dtype: enum
    options: ['0', '1']
    option_labels: [LFDMA, DFDMA]

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ N }

templates:
    imports: import ofdm
    make: ofdm.scfdma_subcarrier_demapper_vcvc(${N}, ${M}, ${start}, ${mode})

file_format: 1
