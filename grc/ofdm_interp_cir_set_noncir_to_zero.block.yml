# auto-generated by grc.converter

id: ofdm_interp_cir_set_noncir_to_zero
label: interp_cir_set_noncir_to_zero
category: ofdm

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import ofdm
    make: ofdm.interp_cir_set_noncir_to_zero(${vlen}, ${window})

file_format: 1
