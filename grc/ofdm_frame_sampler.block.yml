# auto-generated by grc.converter

id: ofdm_frame_sampler
label: OFDM Frame Sampler
category: ofdm

parameters:
-   id: subcarriers
    label: Total Subcarriers
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_length
    label: Frame Length
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
-   domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
-   domain: stream
    dtype: byte

templates:
    imports: from ofdm.ofdm_frame_sampler_grc import ofdm_frame_sampler
    make: ofdm_frame_sampler(${subcarriers}, ${frame_length})

documentation: "OFDM Frame Sampler\n\tWrapper Block"

file_format: 1
