# auto-generated by grc.converter

id: ofdm_lms_phase_tracking_02
label: lms_phase_tracking_02
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: pilot_subc
    label: Pilot Subcarries
    dtype: int_vector
-   id: nondata_blocks
    label: Nondata Blocks
    dtype: int_vector

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import ofdm
    make: ofdm.lms_phase_tracking_02(${vlen}, ${pilot_subc}, ${nondata_blocks})

file_format: 1
