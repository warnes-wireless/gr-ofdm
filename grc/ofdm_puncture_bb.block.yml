# auto-generated by grc.converter

id: ofdm_puncture_bb
label: Puncture
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: byte
-   label: modemap
    domain: stream
    dtype: byte
    vlen: ${ vlen }
-   label: trig
    domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import ofdm
    make: ofdm.puncture_bb(${vlen})

file_format: 1
