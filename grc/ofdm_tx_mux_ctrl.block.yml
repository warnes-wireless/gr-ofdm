# auto-generated by grc.converter

id: ofdm_tx_mux_ctrl
label: Tx Multiplexer Control
category: ofdm

parameters:
-   id: subcarriers
    label: Subcarriers
    dtype: int

inputs:
-   domain: stream
    dtype: int

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import ofdm
    make: ofdm.tx_mux_ctrl(${subcarriers})

file_format: 1
