# auto-generated by grc.converter

id: ofdm_vector_mask_dc_null
label: vector_mask_dc_null
category: ofdm

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import ofdm
    make: ofdm.vector_mask_dc_null(${vlen}, ${cut_left}, ${mask_len}, ${dc_zeros},
        ${mask})

file_format: 1
