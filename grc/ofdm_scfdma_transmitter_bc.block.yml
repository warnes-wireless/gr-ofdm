# auto-generated by grc.converter

id: ofdm_scfdma_transmitter_bc
label: SC-FDMA Transmitter
category: SC-FDMA

parameters:
-   id: N
    label: N
    dtype: int
    default: N
-   id: M
    label: M
    dtype: int
    default: M
-   id: start_index
    label: start_index
    dtype: int
    default: start_index
-   id: mapping
    label: Mapping
    dtype: enum
    options: ['0', '1']
    option_labels: [LFDMA, DFDMA]
-   id: modulation
    label: modulation
    dtype: int
    default: modulation
-   id: cp_ratio
    label: cp_ratio
    dtype: raw
    default: cp_ratio

inputs:
-   domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import ofdm
    make: ofdm.scfdma_transmitter_bc(${N}, ${M}, ${start_index}, ${mapping}, ${modulation},
        ${cp_ratio})

file_format: 1
