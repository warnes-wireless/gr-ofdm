# auto-generated by grc.converter

id: ofdm_divide_frame_fc
label: Divide Frame
category: ofdm

parameters:
-   id: frame_size
    label: Frame Size
    dtype: int
-   id: subc
    label: Subcarriers
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   label: in_samples
    domain: stream
    dtype: complex
    vlen: ${ subc }
-   label: in_power
    domain: stream
    dtype: float
    vlen: ${ subc }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ subc }

templates:
    imports: import ofdm
    make: ofdm.divide_frame_fc(${frame_size}, ${subc})

file_format: 1
