# auto-generated by grc.converter

id: ofdm_fbmc_overlapping_serial_to_parallel_cvc
label: Overlapping S/P
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_overlapping_serial_to_parallel_cvc(${M})

file_format: 1
