# auto-generated by grc.converter

id: ofdm_vector_padding
label: Vector Padding
category: FBMC

parameters:
-   id: carriers
    label: Number of Carriers
    dtype: int
    default: carriers
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: vlen
    label: Vlen
    dtype: int
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: padding_left
    label: Padding_left
    dtype: int
    default: ' -1'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ carriers }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import ofdm
    make: ofdm.vector_padding(${carriers}, ${vlen}, ${padding_left})

file_format: 1
