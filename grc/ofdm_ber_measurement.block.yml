# auto-generated by grc.converter

id: ofdm_ber_measurement
label: BER Measurement
category: ofdm

parameters:
-   id: sliding_window_length
    label: Sliding Window Length
    dtype: int

inputs:
-   domain: stream
    dtype: byte
    multiplicity: '2'

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import ofdm
    make: ofdm.ber_measurement(${sliding_window_length})

file_format: 1
