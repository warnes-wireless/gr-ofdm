# auto-generated by grc.converter

id: ofdm_autocorrelator
label: OFDM Autocorrelator
category: ofdm

parameters:
-   id: lag
    label: Lag
    dtype: int
-   id: n_samples
    label: Number of Samples
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float

templates:
    imports: from ofdm.autocorrelator import autocorrelator
    make: autocorrelator(${lag}, ${n_samples})

file_format: 1
