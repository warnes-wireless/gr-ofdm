# auto-generated by grc.converter

id: ofdm_viterbi_combined_fb
label: Viterbi Combined
category: ofdm

parameters:
-   id: fsm_args
    label: FSM Args
    dtype: raw
-   id: K
    label: Block Size
    dtype: int
-   id: init_state
    label: Initial State
    dtype: int
    default: '0'
-   id: final_state
    label: Final State
    dtype: int
    default: '-1'
-   id: dim
    label: Dimensionality
    dtype: int
-   id: chunkdivisor
    label: Chunk Divisor
    dtype: int
-   id: table
    label: Constellation
    dtype: float_vector
-   id: metric_type
    label: Metric Type
    dtype: enum
    options: [ofdm.TRELLIS_EUCLIDEAN, ofdm.TRELLIS_HARD_SYMBOL, ofdm.TRELLIS_HARD_BIT]
    option_labels: [Euclidean, Hard Symbol, Hard Bit]

inputs:
-   domain: stream
    dtype: float
-   domain: stream
    dtype: int

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import ofdm
    make: ofdm.viterbi_combined_fb(ofdm.fsm(${fsm_args}), ${K}, ${init_state}, ${final_state},
        ${dim}, ${chunkdivisor}, ${table}, ${metric_type})

file_format: 1
