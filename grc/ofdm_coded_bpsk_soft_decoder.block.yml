# auto-generated by grc.converter

id: ofdm_coded_bpsk_soft_decoder
label: Coded BPSK Soft Decoder
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: output_bits
    label: Output Bits
    dtype: int
-   id: whitener
    label: Whitener
    dtype: int_vector

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: short

templates:
    imports: import ofdm
    make: ofdm.coded_bpsk_soft_decoder(${vlen}, ${output_bits}, ${whitener})

file_format: 1
