# auto-generated by grc.converter

id: ofdm_fbmc_snr_filter
label: FBMC SNR Filter
category: FBMC

parameters:
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_length
    label: Frame Length
    dtype: int
    default: '1'
-   id: training_data
    label: Training Data
    dtype: raw

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
asserts:
- ${ subcarriers > 0 }

templates:
    imports: from ofdm.preambles_grc import fbmc_snr_filter
    make: fbmc_snr_filter(${subcarriers}, ${frame_length}, ${training_data})

documentation: |-
    FBMC SNR Filter
        Wrapper Block

file_format: 1
