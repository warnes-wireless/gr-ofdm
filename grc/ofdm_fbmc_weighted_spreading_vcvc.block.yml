# auto-generated by grc.converter

id: ofdm_fbmc_weighted_spreading_vcvc
label: Weighted Spreading
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: K
    label: K
    dtype: raw
    default: K
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   label: in(M)
    domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   label: out(KM)
    domain: stream
    dtype: complex
    vlen: ${ K*M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_weighted_spreading_vcvc(${M}, ${K})

file_format: 1
