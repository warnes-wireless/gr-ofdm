# auto-generated by grc.converter

id: tx_rpc_manager
label: Tx RPC Manager
category: ofdm

parameters:
-   id: fft_length
    label: FFT Length
    dtype: int
    default: fft_length
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: subcarriers
-   id: data_blocks
    label: Data Blocks
    dtype: int
    default: data_blocks
-   id: frame_length
    label: Frame Length
    dtype: int
    default: frame_length
-   id: cp_length
    label: CP Length
    dtype: int
    default: cp_length
-   id: tx_freq
    label: Tx Frequency
    dtype: float
    default: tx_freq
-   id: bandwidth
    label: Bandwidth
    dtype: int
    default: bandwidth

templates:
    imports: from ofdm.tx_rpc_manager_grc import tx_rpc_manager
    make: tx_rpc_manager(${fft_length}, ${subcarriers}, ${data_blocks}, ${frame_length},
        ${cp_length}, ${tx_freq}, ${bandwidth})

documentation: "Tx RPC Manager\n\tWrapper Block"

file_format: 1
