# auto-generated by grc.converter

id: ofdm_channel_equalizer
label: OFDM Channel Equalizer
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import ofdm
    make: ofdm.channel_equalizer(${vlen})

file_format: 1
