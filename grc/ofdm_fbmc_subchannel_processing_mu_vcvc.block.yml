# auto-generated by grc.converter

id: ofdm_fbmc_subchannel_processing_mu_vcvc
label: Subchannel Processing (Multiuser)
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: syms_per_frame
    label: Syms_per_frame
    dtype: raw
    default: syms_per_frame
-   id: indices
    label: Indices
    dtype: raw
    default: Indices
-   id: sel_preamble
    label: sel_preamble
    dtype: enum
    options: ['0', '1', '2']
    option_labels: [IAM-C, IAM-C with triple rep., IAM-R]
-   id: zero_pads
    label: Zero padding
    dtype: int
    default: '1'
-   id: extra_pad
    label: Extra padding
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']
-   id: sel_eq
    label: Equalizer
    dtype: enum
    options: ['0', '1', '2', '3']
    option_labels: [One Tap, 'Three taps, linear interpolation', 'Three taps, geometric
            interpolation', No equalizer]

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }
-   label: estimation
    domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_subchannel_processing_mu_vcvc(${M}, ${syms_per_frame}, ${indices},
        ${sel_preamble}, ${zero_pads}, ${extra_pad}, ${sel_eq})

file_format: 1
