# auto-generated by grc.converter

id: ofdm_pilot_subcarrier_inserter
label: OFDM Pilot Subcarrier Inserter
category: ofdm

parameters:
-   id: data_subcarriers
    label: Data Subcarriers
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: symbols
    label: Pilot Subcarriers
    dtype: float_vector
-   id: subc_index
    label: Pilot Subcarriers Index
    dtype: int_vector
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ data_subcarriers }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ data_subcarriers+len(subc_index) }

templates:
    imports: import ofdm
    make: ofdm.pilot_subcarrier_inserter(${data_subcarriers}, ${symbols}, ${subc_index})

file_format: 1
