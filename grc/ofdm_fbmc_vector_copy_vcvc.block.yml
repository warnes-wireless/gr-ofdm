# auto-generated by grc.converter

id: ofdm_fbmc_vector_copy_vcvc
label: Vector Copy
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: interpolation
    label: Interpolation
    dtype: raw
    default: '2'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_vector_copy_vcvc(${M}, ${interpolation})

file_format: 1
