# auto-generated by grc.converter

id: ofdm_dynamic_trigger_ib
label: Dynamic Trigger
category: ofdm

parameters:
-   id: first_or_last
    label: First Or Last
    dtype: enum
    options: ['1', '0']
    option_labels: ['True', 'False']

inputs:
-   domain: stream
    dtype: int

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import ofdm
    make: ofdm.dynamic_trigger_ib(${first_or_last})

file_format: 1
