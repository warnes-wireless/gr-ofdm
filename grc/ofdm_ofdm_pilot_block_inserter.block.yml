# auto-generated by grc.converter

id: ofdm_ofdm_pilot_block_inserter
label: OFDM Pilot Block Inserter
category: ofdm

parameters:
-   id: frame_length
    label: Frame Length
    dtype: int
    default: frame_length
-   id: fft_length
    label: FFT Length
    dtype: int
    default: fft_length
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: cp_length
    label: CP Length
    dtype: int
    default: cp_length
-   id: training_data
    label: Training Data
    dtype: raw
    default: training_data
-   id: ant
    label: Antennas
    dtype: int
    default: '5'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ fft_length }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ fft_length }
asserts:
- ${ frame_length > 0 }
- ${ fft_length> 0 }
- ${ ant > 0 }

templates:
    imports: from ofdm.preambles_grc import ofdm_pilot_block_inserter
    make: ofdm_pilot_block_inserter(${frame_length}, ${fft_length}, ${cp_length},
        ${training_data}, ${ant})

documentation: "OFDM Pilot Block Inserter\n\tWrapper Block"

file_format: 1
