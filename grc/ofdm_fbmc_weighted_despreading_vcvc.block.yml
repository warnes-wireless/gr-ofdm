# auto-generated by grc.converter

id: ofdm_fbmc_weighted_despreading_vcvc
label: Weighted Despreading
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: K
    label: K
    dtype: raw
    default: K
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   label: in(KM)
    domain: stream
    dtype: complex
    vlen: ${ K*M }

outputs:
-   label: out(M)
    domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_weighted_despreading_vcvc(${M}, ${K})

file_format: 1
