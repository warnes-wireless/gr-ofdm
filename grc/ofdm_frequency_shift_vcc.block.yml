# auto-generated by grc.converter

id: ofdm_frequency_shift_vcc
label: Frequency Shift
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: sensitivity
    label: Sensitivity
    dtype: float
-   id: cp_length
    label: Cyclic Prefix Length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   domain: stream
    dtype: float
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import ofdm
    make: ofdm.frequency_shift_vcc(${vlen}, ${sensitivity}, ${cp_length})

file_format: 1
