# auto-generated by grc.converter

id: ofdm_vector_element
label: Vector Element
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: element
    label: Element
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import ofdm
    make: ofdm.vector_element(${vlen}, ${element})
    callbacks:
    - set_element(int)

file_format: 1
