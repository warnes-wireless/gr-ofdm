# auto-generated by grc.converter

id: ofdm_fbmc_insert_preamble_vcvc
label: Insert Preamble
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: syms_per_frame
    label: syms_per_frame
    dtype: raw
    default: syms_per_frame
-   id: sel_preamble
    label: sel_preamble
    dtype: enum
    options: ['0', '1', '2']
    option_labels: [IAM-C, IAM-C with triple rep., IAM-R]
-   id: zero_pads
    label: Zero padding
    dtype: int
    default: '1'
-   id: extra_pad
    label: Extra padding
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_insert_preamble_vcvc(${M}, ${syms_per_frame}, ${sel_preamble},
        ${zero_pads}, ${extra_pad})

file_format: 1
