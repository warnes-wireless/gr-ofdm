# auto-generated by grc.converter

id: ofdm_fbmc_receiver_hier_cb
label: Receiver
category: FBMC

parameters:
-   id: M
    label: M
    dtype: int
    default: M
-   id: K
    label: K
    dtype: int
    default: K
-   id: qam_size
    label: qam_size
    dtype: int
    default: qam_size
-   id: syms_per_frame
    label: syms_per_frame
    dtype: int
    default: syms_per_frame
-   id: theta_sel
    label: theta_sel
    dtype: int
    default: theta_sel
-   id: carriers
    label: Number of Carriers
    dtype: int
    default: carriers
-   id: exclude_preamble
    label: exclude_preamble
    dtype: int
    default: exclude_preamble
-   id: sel_preamble
    label: sel_preamble
    dtype: enum
    options: ['0', '1', '2']
    option_labels: [IAM-C, IAM-C with triple rep., IAM-R]
-   id: zero_pads
    label: Zero padding
    dtype: int
    default: '1'
-   id: extra_pad
    label: Extra padding
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']
-   id: sel_eq
    label: sel_eq
    dtype: enum
    options: ['0', '1', '2', '3']
    option_labels: [One Tap, 'Three taps, linear interpolation', 'Three taps, geometric
            interpolation', No equalizer]

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: byte
    vlen: 1

templates:
    imports: import ofdm
    make: ofdm.fbmc_receiver_hier_cb(${M}, ${K}, ${qam_size}, ${syms_per_frame}, ${carriers},
        ${theta_sel}, ${sel_eq}, ${exclude_preamble}, ${sel_preamble}, ${zero_pads},
        ${extra_pad})

file_format: 1
