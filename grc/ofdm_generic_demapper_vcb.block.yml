# auto-generated by grc.converter

id: ofdm_generic_demapper_vcb
label: Generic Demapper
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_size
    label: Frame Size
    dtype: int

inputs:
-   label: sym_in
    domain: stream
    dtype: complex
    vlen: ${ vlen }
-   label: cv_in
    domain: stream
    dtype: byte
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import ofdm
    make: ofdm.generic_demapper_vcb(${vlen}, ${frame_size})

file_format: 1
