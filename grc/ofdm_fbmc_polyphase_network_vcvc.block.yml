# auto-generated by grc.converter

id: ofdm_fbmc_polyphase_network_vcvc
label: Polyphase Network
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: K
    label: K
    dtype: raw
    default: K
-   id: reverse
    label: Synthesis/Analysis
    dtype: enum
    options: ['False', 'True']
    option_labels: [Synthesis, Analysis]
-   id: lp
    label: Filter length
    dtype: raw
    default: K*M-1

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_polyphase_network_vcvc(${M}, ${K}, ${lp}, ${reverse})

file_format: 1
