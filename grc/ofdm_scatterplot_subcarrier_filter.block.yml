# auto-generated by grc.converter

id: scatterplot_subcarrier_filter
label: Scatterplot Subcarrier Filter
category: ofdm

parameters:
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: subcarriers
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: data_blocks
    label: Data Blocks
    dtype: int
    default: data_blocks

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
asserts:
- ${ subcarriers > 0 }

templates:
    imports: from ofdm.preambles_grc import scatterplot_subcarrier_filter
    make: scatterplot_subcarrier_filter(${subcarriers}, ${data_blocks})

documentation: |-
    Scatterplot Subcarrier Filter
        Wrapper Block

file_format: 1
