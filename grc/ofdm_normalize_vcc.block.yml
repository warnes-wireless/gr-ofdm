# auto-generated by grc.converter

id: ofdm_normalize_vcc
label: normalize_vcc
category: ofdm

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import ofdm
    make: ofdm.normalize_vcc(${vlen}, ${norm_power})

file_format: 1
