# auto-generated by grc.converter

id: ofdm_snr_filter
label: OFDM SNR Filter
category: ofdm

parameters:
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_length
    label: Frame Length
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers }
asserts:
- ${ subcarriers > 0 }

templates:
    imports: from ofdm.preambles_grc import ofdm_snr_filter
    make: ofdm_snr_filter(${subcarriers}, ${frame_length})

documentation: |-
    OFDM SNR Filter
        Wrapper Block

file_format: 1
