# auto-generated by grc.converter

id: ofdm_fbmc_frame_sampler
label: FBMC Frame Sampler
category: FBMC

parameters:
-   id: total_subcarriers
    label: Total Subcarriers
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: frame_length
    label: Frame Length
    dtype: int
    default: '1'
-   id: frame_data_part
    label: Frame Data Part
    dtype: int
    default: '1'
-   id: training_data
    label: Training Data
    dtype: raw

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ total_subcarriers }
-   domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ total_subcarriers }
-   domain: stream
    dtype: byte

templates:
    imports: from ofdm.fbmc_frame_sampler_grc import fbmc_frame_sampler
    make: fbmc_frame_sampler(${total_subcarriers}, ${frame_length}, ${frame_data_part},
        ${training_data})

documentation: "FBMC Frame Sampler\n\tWrapper Block"

file_format: 1
