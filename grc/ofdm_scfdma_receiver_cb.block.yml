# auto-generated by grc.converter

id: ofdm_scfdma_receiver_cb
label: SC-FDMA Receiver
category: SC-FDMA

parameters:
-   id: N
    label: N
    dtype: int
    default: N
-   id: M
    label: M
    dtype: int
    default: M
-   id: num_users
    label: Num Users
    dtype: int
    default: len(indices)
    hide: part
-   id: indices
    label: Start indices
    dtype: raw
    default: indices
-   id: mapping
    label: Mapping
    dtype: enum
    options: ['0', '1']
    option_labels: [LFDMA, DFDMA]
-   id: modulation
    label: modulation
    dtype: int
    default: modulation
-   id: cp_ratio
    label: cp_ratio
    dtype: raw
    default: cp_ratio

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: byte
    vlen: 1
    multiplicity: ${ num_users }

templates:
    imports: import ofdm
    make: ofdm.scfdma_receiver_cb(${N}, ${M}, ${indices}, ${mapping}, ${modulation},
        ${cp_ratio})

file_format: 1
