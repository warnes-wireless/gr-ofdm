# auto-generated by grc.converter

id: ofdm_find_cir_shift
label: find_cir_shift
category: ofdm

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import ofdm
    make: ofdm.find_cir_shift(${vlen}, ${cir_len})

file_format: 1
