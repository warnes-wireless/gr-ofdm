# auto-generated by grc.converter

id: ofdm_fbmc_symbol_estimation_vcb
label: Symbol Estimation
category: FBMC

parameters:
-   id: M
    label: M
    dtype: int
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: qam_size
    label: qam_size
    dtype: int
    default: qam_size

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: byte
    vlen: 1

templates:
    imports: import ofdm
    make: ofdm.fbmc_symbol_estimation_vcb(${M}, ${qam_size})

file_format: 1
