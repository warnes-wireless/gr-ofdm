# auto-generated by grc.converter

id: ofdm_depuncture_ff
label: Depuncture FF
category: ofdm

parameters:
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: fillval
    label: Filling Value
    dtype: float

inputs:
-   domain: stream
    dtype: float
-   label: modemap
    domain: stream
    dtype: byte
    vlen: ${ vlen }
-   label: trig
    domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import ofdm
    make: ofdm.depuncture_ff(${vlen}, ${fillval})

file_format: 1
