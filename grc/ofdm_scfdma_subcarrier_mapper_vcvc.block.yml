# auto-generated by grc.converter

id: ofdm_scfdma_subcarrier_mapper_vcvc
label: Subcarrier Mapper
category: SC-FDMA

parameters:
-   id: N
    label: N
    dtype: int
    default: N
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: M
    label: M
    dtype: int
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: start
    label: Start index
    dtype: int
-   id: mode
    label: Mode
    dtype: enum
    options: ['0', '1']
    option_labels: [LFDMA, DFDMA]

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ N }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.scfdma_subcarrier_mapper_vcvc(${N}, ${M}, ${start}, ${mode})

file_format: 1
