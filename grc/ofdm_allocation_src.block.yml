# auto-generated by grc.converter

id: ofdm_allocation_src
label: Allocation Source
category: ofdm

parameters:
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: subcarriers
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: data_symbols
    label: Data Symbols
    dtype: int
    default: data_symbols
-   id: address
    label: Address
    dtype: string
    default: address
-   id: coding
    label: Coding
    dtype: raw
    default: coding
    hide: part

outputs:
-   label: id
    domain: stream
    dtype: short
    vlen: 1
-   label: power
    domain: stream
    dtype: float
    vlen: ${ subcarriers }
-   label: bitloading
    domain: stream
    dtype: byte
    vlen: ${ subcarriers }
-   label: bitcount
    domain: stream
    dtype: int
    vlen: 1
    multiplicity: ${ coding+1 }

templates:
    imports: import ofdm
    make: ofdm.allocation_src(${subcarriers}, ${data_symbols}, ${address}, ${coding})
    callbacks:
    - set_allocation(char_vector, float_vector)

file_format: 1
