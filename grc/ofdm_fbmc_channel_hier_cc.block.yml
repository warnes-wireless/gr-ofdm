# auto-generated by grc.converter

id: ofdm_fbmc_channel_hier_cc
label: Channel Model (FBMC)
category: FBMC

parameters:
-   id: M
    label: M
    dtype: int
    default: M
-   id: K
    label: K
    dtype: int
    default: K
-   id: syms_per_frame
    label: syms_per_frame
    dtype: int
    default: syms_per_frame
-   id: exclude_multipath
    label: Multipath Effects
    dtype: enum
    options: ['0', '1']
    option_labels: ['On', 'Off']
-   id: sel_taps
    label: Channel Type
    dtype: enum
    options: ['0', '1', '2']
    option_labels: [EPA, EVA, ETU]
-   id: freq_offset
    label: Frequency Offset
    dtype: float
    default: '0'
-   id: exclude_noise
    label: Noise
    dtype: enum
    options: ['0', '1']
    option_labels: ['On', 'Off']
-   id: sel_noise_type
    label: Noise Type
    dtype: enum
    options: ['201', '200', '202', '203']
    option_labels: [Gaussian, Uniform, Laplacian, Impulse]
-   id: SNR
    label: SNR
    dtype: real
    default: SNR
-   id: exclude_preamble
    label: exclude_preamble
    dtype: int
    default: exclude_preamble
-   id: sel_preamble
    label: sel_preamble
    dtype: enum
    options: ['0', '1', '2']
    option_labels: [IAM-C, IAM-C with triple rep., IAM-R]
-   id: zero_pads
    label: Zero padding
    dtype: int
    default: '1'
-   id: extra_pad
    label: Extra padding
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import ofdm
    make: ofdm.fbmc_channel_hier_cc(${M}, ${K}, ${syms_per_frame}, ${exclude_multipath},
        ${sel_taps}, ${freq_offset}, ${exclude_noise}, ${sel_noise_type}, ${SNR},
        ${exclude_preamble}, ${sel_preamble}, ${zero_pads}, ${extra_pad})
    callbacks:
    - set_SNR(${SNR})

file_format: 1
