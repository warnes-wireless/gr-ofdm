# auto-generated by grc.converter

id: ofdm_cyclic_prefixer
label: Cyclic Prefixer
category: ofdm

parameters:
-   id: input_size
    label: Input Size
    dtype: raw
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: output_size
    label: Output Size
    dtype: raw

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ input_size }

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import ofdm
    make: ofdm.cyclic_prefixer(${input_size}, ${output_size})

file_format: 1
