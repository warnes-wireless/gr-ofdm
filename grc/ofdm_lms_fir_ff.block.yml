# auto-generated by grc.converter

id: ofdm_lms_fir_ff
label: LMS FIR
category: ofdm

parameters:
-   id: len
    label: Length
    dtype: int
-   id: mu
    label: MU Parameter
    dtype: float

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import ofdm
    make: ofdm.lms_fir_ff(${len}, ${mu})

file_format: 1
