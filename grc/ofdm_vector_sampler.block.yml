# auto-generated by grc.converter

id: ofdm_vector_sampler
label: Vector Sampler
category: ofdm

parameters:
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part
-   id: itemsize
    label: itemsize
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: vlen
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   label: iptr
    domain: stream
    dtype: ${ type }
    vlen: ${ itemsize }
-   label: trigger
    domain: stream
    dtype: byte
    vlen: 1

outputs:
-   label: optr
    domain: stream
    dtype: ${ type }
    vlen: ${ itemsize*vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: import ofdm
    make: ofdm.vector_sampler(${type.size}*${itemsize}, ${vlen})

file_format: 1
