# auto-generated by grc.converter

id: ofdm_fbmc_oqam_postprocessing_vcvc
label: OQAM postprocessing
category: FBMC

parameters:
-   id: M
    label: M
    dtype: raw
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: offset
    label: Offset
    dtype: int
    default: '0'
-   id: theta_sel
    label: Theta selection
    dtype: int
    default: '0'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }

templates:
    imports: import ofdm
    make: ofdm.fbmc_oqam_postprocessing_vcvc(${M}, ${offset}, ${theta_sel})

file_format: 1
