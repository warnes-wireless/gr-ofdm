# auto-generated by grc.converter

id: ofdm_lms_phase_tracking_dc_null
label: lms_phase_tracking_dc_null
category: ofdm

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import ofdm
    make: ofdm.lms_phase_tracking_dc_null(${vlen}, ${pilot_subc}, ${nondata_blocks},
        ${pilot_subcarriers}, ${dc_null})

file_format: 1
