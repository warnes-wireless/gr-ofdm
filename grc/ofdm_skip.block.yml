# auto-generated by grc.converter

id: ofdm_skip
label: Skip
category: ofdm

parameters:
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part
-   id: blocklen
    label: Block Length
    dtype: int
    default: '1'
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
-   label: trigger
    domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
asserts:
- ${ vlen >= 1 }

templates:
    imports: import ofdm
    make: ofdm.skip(${type.size}*${vlen}, ${blocklen})

file_format: 1
