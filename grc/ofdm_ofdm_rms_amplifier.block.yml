# auto-generated by grc.converter

id: ofdm_rms_amplifier
label: OFDM RMS Amplifier
category: ofdm

parameters:
-   id: amplitude
    label: Amplitude
    dtype: float
    default: '1'
-   id: subcarriers
    label: Subcarriers
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: from ofdm.ofdm_rms_amplifier_grc import ofdm_rms_amplifier
    make: ofdm_rms_amplifier(${amplitude}, ${subcarriers})
    callbacks:
    - set_rms_amplitude(${amplitude})

documentation: "OFDM RMS Amplifier\n\tWrapper Block"

file_format: 1
