# auto-generated by grc.converter

id: ofdm_fbmc_overlapping_parallel_to_serial_vcc
label: Overlapping P/S
category: FBMC

parameters:
-   id: M
    label: M
    dtype: int
    default: M
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ M }
    multiplicity: '2'

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import ofdm
    make: ofdm.fbmc_overlapping_parallel_to_serial_vcc(${M})

file_format: 1
