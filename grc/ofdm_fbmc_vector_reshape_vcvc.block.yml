# auto-generated by grc.converter

id: ofdm_fbmc_vector_reshape_vcvc
label: Vector Reshape
category: FBMC

parameters:
-   id: len_in
    label: Input Length
    dtype: raw
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: len_out
    label: Output Length
    dtype: raw
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ len_in }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ len_out }

templates:
    imports: import ofdm
    make: ofdm.fbmc_vector_reshape_vcvc(${len_in}, ${len_out})

file_format: 1
