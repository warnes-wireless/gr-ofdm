# auto-generated by grc.converter

id: ofdm_repetition_encoder_sb
label: Repetition Encoder
category: ofdm

parameters:
-   id: input_bits
    label: Input Bits
    dtype: int
-   id: repetitions
    label: Repetitions
    dtype: int
-   id: whitener
    label: Whitener
    dtype: int_vector

inputs:
-   domain: stream
    dtype: short

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import ofdm
    make: ofdm.repetition_encoder_sb(${input_bits}, ${repetitions}, ${whitener})

file_format: 1
